{
  "name": "tree-sitter-scar",
  "version": "0.1.0",
  "description": "Scar grammar for tree-sitter",
  "main": "bindings/node",
  "scripts": {
    "build": "tree-sitter generate && node-gyp build",
    "test": "tree-sitter test"
  },
  "dependencies": {
    "nan": "^2.17.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.21.0"
  },
  "tree-sitter": [
    {
      "scope": "source.scar",
      "file-types": ["scar"],
      "highlights": "queries/highlights.scm"
    }
  ]
}